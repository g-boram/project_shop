import { getBoardList } from '@/remote/board'
import { useQuery } from 'react-query'

// 게시판 데이터 가져오기
// export function useCategoryBoardData() {
//   return useQuery('useCosmeticData', getBoardList, {
//     select: (data) => data.filter((v) => v.category === category),
//   })
// }
